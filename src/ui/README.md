# ui

Navigation UI over the index, plus the relationship-graph visualization and the
query view ("look up an item, see relationships and evidence").

Must have tests (capability #5 in the project README).

Status: not implemented.
